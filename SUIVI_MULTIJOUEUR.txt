================================================================
SUIVI MULTIJOUEUR — Hyperion Orbit (Darkorbit LOL)
Dernière mise à jour : 2026 (session multi + comptes serveur)
================================================================

BUT DU PROJET : passer du jeu solo local à un vrai jeu en ligne :
mêmes NPC pour tous, vaisseaux visibles, tirs visibles, morts partagées,
comptes sur serveur. Testé à 2 (navigateur normal + navigation privée).

----------------------------------------------------------------
1) BASE RESEAU (joueurs qui se voient)
----------------------------------------------------------------
- Serveur : SCRIPTS/MULTI_SERVER.js (HTTP statique + WebSocket /ws).
  Lancement : npm run server:multi  (port 8080 par défaut, --port= possible)
  Dépendance ajoutée : ws (package.json).
- Client : SRC/CORE/NETPLAY.js (envoi position 20 Hz, snapshots, extrapolation courte).
- Moteur : SRC/CORE/ORBIT_ENGINE.js (16 lignes au début, puis extensions) :
  envoi état local chaque frame + dessin des distants.
- Protocole pos : x, y, angle, shipId, pseudo, dead, hpPct, shPct, atk,
  tx/ty (point visé), ammo, drones/dform/dslots, safe/hidden, rank/firm,
  dind/ficon/mind (+ anciens champs shots/alt/fint/bspd/rseq/rkind/rspd
  encore relayés mais plus utilisés depuis les événements de tir).
- Rendu distant : vrai sprite vaisseau (bon sens via angleOffset),
  réacteurs (ShipEngine), drones (vrais sprites + formation), balancement
  à l'arrêt, plaque complète via drawPlayerStatus (grade + nom + firme en
  dessous ; barres HP/shield + carrés drones + carrés modules + formation
  au-dessus), minimap (point bleu même firme, rouge sinon, taille NPC +
  portée radar comme les NPC).
- Solo-safe : sans WS joignable, le jeu reste 100 % solo.

----------------------------------------------------------------
2) TIRS VISUELS (événements exacts, zéro dégât)
----------------------------------------------------------------
- Le tireur envoie chaque salve matérialisée (vraies + faux tirs de
  transition) : {t:"shot", key, n(1|2), x, y, ang, tx, ty, spd, sab}
  depuis tryFireOnce et spawnSalvoDecoy.
- Roquettes : {t:"rshot", kind, x, y, ang, tx, ty, spd, arcDir, arcScale,
  arcBoost, prange} depuis spawnRocketProjectile (arcs identiques,
  lance-roquettes inclus, fumées auto via isRocket).
- Serveur : retransmet tels quels à la room (sauf l'envoyeur), sans état.
- Receveur (tickNetplayVisuals) : projectiles visuels (mêmes sprites,
  homing + impacts silencieux, SAB inversé comme en local).
- Ancien système (compteur shots + faisceau violet) supprimé côté réception.

----------------------------------------------------------------
3) NPC PARTAGES (serveur autoritaire)
----------------------------------------------------------------
- Serveur : SCRIPTS/NPC_ROOM.js (ZoneNpcSim). Réutilise les modules purs :
  UNIVERSE_SIM (slots/respawn), NPC_TYPES (stats), COMBAT_RULES
  (damageEnemyLayers), MAPS/<id>/SPAWNS.js + WORLD.js (camps, zones sûres).
- Tick 20 Hz : respawn, dérive/orbite/poursuite/fuite, kamikazes.
- Client : prédiction locale + snapshots serveur (20 Hz) qui corrigent.
  Hook unique dégâts dans damageEnemy + drainShieldFromEnemy -> sendNetHit.
- Morts : journal des kills (2,5 s) + n° d'incarnation (seq) anti-confusion
  avec le respawn instantané. Kill = PREMIER attaquant (firstBy, repli
  dernier si parti). Killer seul : explosion + cargo + crédits/XP/quêtes.
  Autre : explosion + retrait silencieux. Lock libéré à la mort.
- Aggro partagée (snapshot) : les 2 écrans voient le NPC tirer la même
  cible. Tirs vers le copain = visuels (vol droit, disparition silencieuse,
  zéro dégât). Flag _attackedPlayerRecently synchronisé (sinon points
  rouges persistants au loin sur la minimap).
- Comportements parité solo : dérive, orbite en poursuite proche (jamais
  statique), poursuite agresseur + proximité (rayon camp), passifs
  (provoqués seulement), zone sûre (aucune poursuite/tir, sortie + spawn
  et dérive hors zone, glissade anti-glitch aux bords), fuite <10 % PV
  (rattrapable, tirs continuent), kamikaze (explosion + dégâts de zone +
  récompense victime), camouflage (ignorés).
- Boucle mouvement locale sautée pour les NPC réseau (positions serveur),
  tirs NPC locaux conservés. Purge des doublons locaux (universeUid).
- Repli solo : si snapshots >3 s ou map non-zone, spawner local reprend.

----------------------------------------------------------------
4) DEGATS PARTAGES VISIBLES
----------------------------------------------------------------
- Serveur : feed dégâts agrégés par (npc, attaquant) et par tick,
  inclus dans le snapshot (dmg:[{uid, by, total}], max 24).
- Client : chiffres flottants via queueVolleyFloat UNIQUEMENT si on locke
  la même cible (Target._netUid === uid). Les barres HP suivent toujours
  le snapshot dans tous les cas.
- One-shot : pas de chiffres (mort immédiate), normal (explosion + loot).

----------------------------------------------------------------
5) BONUS BOX PARTAGEES
----------------------------------------------------------------
- Hôte élu = plus petit id de la room (champ host du snapshot).
- Hôte seul spawne (store local) + publie sa liste chaque seconde.
- Autres : aucun spawn, instances miroirs (_netBox, sans store).
- Collecte : récompense personnelle + event immédiat ; premier arrivé.
  L'hôte tue aussi son slot (respawn partagé à venir).
- Serveur : set par room, boxesSync à l'arrivée, events collect/list,
  unspawn au départ, anti-spoof (seul l'hôte publie).
- Cargos de NPC : PAS partagés (seul le tueur les voit) — voulu pour l'instant.
- Diagnostic : window.__NETBOXDBG__ (hôte, tailles, listes envoyées).

----------------------------------------------------------------
6) COMPTES SERVEUR (SQLite, zéro dépendance)
----------------------------------------------------------------
- Serveur : SCRIPTS/ACCOUNT_SERVER.js, DB SERVER_DATA/orbit.db (GITIGNORE,
  à SAUVEGARDER sur le serveur maison !). node:sqlite (expérimental Node 24).
- API : GET /api/ping · POST /api/register {pseudo,email,password,faction,
  migrate?} · POST /api/login · GET /api/me · POST /api/save {user} ·
  POST /api/logout. Tokens Bearer 30 jours. Rate-limit 20/min/IP.
- Mots de passe : même format que le client (v1$salt$SHA256). Casse pseudo
  préservée (colonne pseudo_norm unique). Révision strictement croissante
  (409 stale + canon si en retard, anti-écrasement). Conflit pseudo/email :
  200 + canon partiel (le serveur tranche).
- Migration unique : inscription avec même pseudo/email + mot de passe
  prouve la possession du compte local -> progression importée.
- Client : SRC/CORE/ACCOUNT_NET.js (token orbit_token + cache orbit_user_cache
  + debounce save 2 s + flush pagehide + refresh /api/me au boot).
  ACCOUNT.js : readUsers/writeUsers/readCurrent/writeCurrent branchent
  serveur si token (saveUser et tout le jeu inchangés). Logout coupe le token.
  PUBLIC/AUTH.js : mode en ligne si serveur joignable (sinon local 100 %
  comme avant) + import local à l'inscription.
- Validé : 14 checks API (doublons, 401/403/404/409, isolation A/B,
  migration, casse) + tests navigateur (2 comptes, persistance après
  fermeture navigateur, indépendance). Solo sans serveur : intact.

----------------------------------------------------------------
PROCEDURE DE TEST LOCALE (à 2)
----------------------------------------------------------------
1. npm run server:multi  (Ctrl+C + relance SEULEMENT si SCRIPTS/ change)
2. 2 fenêtres : normale + privée, http://127.0.0.1:8080/?map=1-1
3. 2 comptes différents (firme différente conseillée pour la minimap)
4. Ctrl+F5 des 2 côtés à chaque changement client (pas besoin sinon)
5. Diagnostic console : window.__NETBOXDBG__ (box), window.__NETHITS__
   (hits envoyés), window.__NETPLAY__.netplayStatus()
6. Console serveur : [multi:npc] hits reçus toutes les 30 s

----------------------------------------------------------------
RESTE A FAIRE (prochaines étapes proposées)
----------------------------------------------------------------
[ ] Transférer sur le serveur maison (Xeon 24x, 44 Go) : dossier + npm
    install + port 8080 + test à 2 vrais PC. SAUVEGARDER SERVER_DATA/.
[x] Dégâts NPC partagés subis validés côté serveur (pool PV/bouclier autoritaire).
[x] Défenses serveur : ISH, Représailles, Fortification, Endurance prismatique,
    Spectrum/Plus, Éventreur, Assimilation et Dernier rempart.
[x] Fluidité : tick 20 Hz + vitesse réelle des joueurs, extrapolation plafonnée à 50 ms.
[ ] PvP (tirs entre joueurs réels) + chat.
[ ] Galaxy Gates / quêtes coop / cargos partagés.
[ ] Durcissement anti-triche (vitesses, cadences, positions).

----------------------------------------------------------------
7) ENCHERES PARTAGEES (comme le tchat global)
----------------------------------------------------------------
- Serveur : SCRIPTS/AUCTION_ROOM.js (lots canoniques 22/heure Paris,
  mises temps réel, règlement à :00, persistances
  SERVER_DATA/auction_state.json). Compte authentifié requis pour miser
  (anti-usurpation), mini +2 % (+1000), anti-spam 800 ms, pseudos des
  miseurs conservés (8 derniers/lot, "qui a parié").
- MULTI_SERVER.js : auctionSync au hello, auctionBid -> broadcast
  auctionUpdate / reject, clôture toutes les 5 s (settle + nouveau sync).
- Client : SRC/CORE/AUCTION_NET.js (fusion état serveur dans
  user.auction.lots, remboursement si surenchère, règlement gains via
  ACCOUNT.js, repli solo si serveur muet >2 min). NETPLAY.js : inbox +
  sendAuctionBid. ORBIT_ENGINE.js : pompe partagée avant le tick local
  (tick local suspendu en partagé). UI : pseudos en direct + "Aussi
  misé : ..." + nb mises, déjà-possédés masqués, ligne dorée recent.
- Crédits réservés côté client (même confiance qu'avant, pas de
  débit serveur). Solo sans serveur : intact.
- Validé : 10 checks live (2 comptes + invité : sync, mini, surenchère
  visible, recents, anti-spam) + boot navigateur sans erreur JS.
  SMOKE_BROWSER minimaliste échoue déjà sans ces changements
  (404 /api/rankings + /ws) : pré-existant, non lié.

----------------------------------------------------------------
8) GATES = INSTANCES PERSO CONNECTÉES (tchat + enchères en gate)
----------------------------------------------------------------
- Avant : socket coupé en gate (suspendNetplay). Maintenant : socket
  gardé, canaux globaux vivants, gameplay 100 % local comme avant.
- Serveur : flag instance:true (hello/map) -> hors room (invisible,
  pas de NPC partagés, pas de PvP, pos/tirs ignorés) ; broadcastAll
  vers TOUS les sockets (tchat + enchères + annonces admin même en
  gate) ; heartbeat enchères 60 s ; log boot (22 lots).
- Client : setNetInstanceMode(!isZoneMap) : purge gameplay (distants,
  NPC, box, tirs, PvP) à l'entrée, aucun envoi gameplay en gate
  (pos/tirs/hits/loot/box/abilités), tchat + mises toujours envoyés.
  Repli solo enchères seulement si socket coupé (plus en gate calme).
- Classement + sauvegarde déjà HTTP : inchangés, OK en gate.
- Limite : panneau admin /peers ne liste pas les joueurs en gate
  (roomless) ; mute/tchat les couvrent quand même.
- Validé : 8 checks live (visible avant, invisible en gate, tchat x2,
  enchères x2 avec pseudos, pos/tirs ignorés, revisible en sortie)
  + boot navigateur sans erreur JS.

----------------------------------------------------------------
9) VRAI JEU MULTI : SAS D'ENTRÉE + HEARTBEAT (bloquant, fini le solo)
----------------------------------------------------------------
- Sas DÉPART : bouton CONNEXION… jusqu'au hello traité (auctionSync),
  sinon RÉESSAYER + "Serveur injoignable". Pas de pos avant started
  (client) + serveur ignore les jamais-positionnés (_posOk) des
  snapshots et de la simu NPC : fini les spawns fantômes (ni vu,
  ni aggro, ni dégâts avant apparition).
- Heartbeat : ping 3 s / pong ; mort déclarée si socket coupé ou
  silence >9 s (pas quand l'onglet est masqué). Liaison morte =
  overlay #linkOverlay bloquant + simulation gelée (ni mouvement
  ni mort), reconnexion forcée toutes les 3 s jusqu'au succès, puis
  reprise (room rejointe via le hello). Enchères gelées aussi (pas
  de repli solo : les mises orphelines sont remboursées au re-sync).
- Valide aussi en gates (socket gardé) : tchat/enchères continuent,
  gameplay gate gelé si la liaison tombe.
- Conséquence : server:multi OBLIGATOIRE pour jouer, même seul.
- Validé : entrée bloquée sans serveur (navigateur), entrée OK +
  overlay à la coupure + reprise au restart (navigateur), fantôme
  invisible puis visible après pos + ping/pong (protocole), boot
  sans erreur JS.

----------------------------------------------------------------
10) ESCADRILLES (groupes) + AMIS + MURMURES
----------------------------------------------------------------
- Groupes éphémères cross-map (max 10, chef = créateur) :
  SCRIPTS/SOCIAL_ROOM.js (pur, injecté). Solo = pas de groupe (pas de
  bouton Créer) : l'invitation crée l'escadrille auto, et retomber à 1
  membre (départ/kick/refus/quit) la clôture (tout le monde en solo).
  Invites 60 s par pseudo, accepter/refuser, quitter, kick (chef seul),
  succession auto au départ du chef, chat de groupe (500 ms anti-spam).
  Avis rejoints/quittés/invitation supprimés (seuls restent les erreurs
  utiles : plein, chef seul, refus, exclusion).
- Serveur (MULTI_SERVER.js) : relais group*/whisper avant les autres
  handlers ; registre instancePeers (joueurs en gate joignables pour les
  messages dirigés) ; findByPseudo/describePeer/sendToPeer (rooms +
  instances) ; resync groupUpdate au hello/map ; purge groupe au close,
  à l'expire 10 s et au kick admin ; présence amis (friendsSync au hello
  authentifié + friendOnline aux abonnés à chaque connexion/cut).
- Amis façon DO (ACCOUNT_SERVER.js, comptes uniquement) : tables friends
  + friend_requests (persistées : un hors-ligne retrouve ses demandes).
  Demande -> acceptation -> amitié mutuelle (les 2 sens) ; demande
  croisée = amitié immédiate ; retrait bilatéral. Routes : GET/POST/
  DELETE /api/friends (POST = demande, plus d'ajout direct),
  GET /api/friends/requests, POST /api/friends/accept|decline
  (rate-limit 60/min).
- Murmures : {t:"whisper", to, text} (800 ms anti-spam), livrés même en
  gate, accusé whisperSent à l'envoyeur.
- Client : NETPLAY.js (getNetGroup/getNetFriendsOnline + inboxes +
  sendGroup*/sendWhisper ; groupMsg et MP miroirs dans le tchat avec
  préfixes [Groupe]/[MP]).
- Fenêtre Groupe (UI/UI_GROUP.js, groupWindow, icône group.png) : statut
  escadrille, membres + carte + 👑 + exclusion (chef), créer/quitter,
  invitation par pseudo, invitations reçues (Rejoindre/Refuser), avis
  système. Pas de champ d'envoi (volontaire) : on parle depuis le tchat.
  Même fabrication que le tchat (Panel/Status/Entries/Empty + reg + init).
- Fenêtre Amis (UI/UI_FRIENDS.js, friendsWindow, icône contacts.png) :
  liste + pastille en ligne live, champ d'ajout (clic sur un nom le
  pré-remplit : lignes Amis, membres du Groupe, et vaisseau du joueur
  directement sur la carte au moment du lock), section "Demandes
  reçues" avec Accepter/Refuser, MP (pré-remplit /w dans le tchat),
  +Grp (invite escadrille). L'autre est notifié live (WS friendRequest
  vérifié en base + ligne [Amis] dans son tchat) et via le poll.
  Refresh 30 s + à la réouverture (comme le classement).
- Murmures façon DO : commande /w pseudo message dans le tchat
  (UI_CHAT.js -> sendWhisper), visibles comme MP entrants/sortants.
- index.html (groupWindow + friendsWindow) + style.css (.group* +
  .friends*) + ORBIT_ENGINE (2x reg + init). Solo sans serveur : intact.
- Validé : 11 checks WS live groupes (create/invite/accept/chat/whisper/
  leave/inconnue/kick, 2 invités) + 14 checks intégration amis en serveur
  isolé (demande/doublon/soi/inconnu/demande visible/live notify/accept
  mutuel + notify/déjà-amis/retrait bilatéral/refus/croisée). Boot
  navigateur à confirmer à 2 (procédure § TEST ci-dessous).
- Limites V1 : pas de partage XP/butin de groupe, pas de pastilles
  groupe sur minimap (la map des membres est déjà dans groupUpdate).

----------------------------------------------------------------
FICHIERS CLES
----------------------------------------------------------------
Créés : SCRIPTS/MULTI_SERVER.js, SCRIPTS/NPC_ROOM.js,
  SCRIPTS/ACCOUNT_SERVER.js, SCRIPTS/AUCTION_ROOM.js, SCRIPTS/SOCIAL_ROOM.js,
  SRC/CORE/NETPLAY.js, SRC/CORE/ACCOUNT_NET.js, SRC/CORE/AUCTION_NET.js,
  UI/UI_GROUP.js, UI/UI_FRIENDS.js
Modifiés : SRC/CORE/ORBIT_ENGINE.js (hooks multi), SRC/CORE/ACCOUNT.js
  (backend net), PUBLIC/AUTH.js (en ligne d'abord), UI/UI_MINIMAP.js
  (points alliés), UI/UI_AUCTION.js (pseudos en direct),
  SRC/DATA/AUCTION.js (champ recent), package.json (ws + script server:multi), .gitignore
  (SERVER_DATA/).
Runtime (non git) : SERVER_DATA/orbit.db, SERVER_DATA/auction_state.json, node_modules/.
================================================================
